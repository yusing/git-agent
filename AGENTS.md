# AGENTS.md

`git-agent` is an OpenAI-compatible, read-only tool-calling agent harness for
Git-related operations.

This tool is general and should not apply any project specific logic/special handling, for more details read `README.md` and `docs/spec.md`.

Update related docs when behavior changed/added.
